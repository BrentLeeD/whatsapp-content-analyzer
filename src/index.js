import React from 'react';
import ReactDOM from 'react-dom/client';
import WhatsAppAnalyzer from './WhatsAppAnalyzer';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <WhatsAppAnalyzer />
  </React.StrictMode>
);